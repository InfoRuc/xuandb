SET(pf_srcs pf_)
SET(MINIDB_INCLUDE_DIRS ${PROJECT_SOURCE_DIR}/include)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin/lib)

INCLUDE_DIRECTORIES(
    ${MINIDB_INCLUDE_DIRS}
)

# ADD_LIBRARY(pf STATIC ${pf_srcs})
# ADD_LIBRARY(pf SHARED ${pf_srcs})

