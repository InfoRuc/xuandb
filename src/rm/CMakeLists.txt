SET(rm_srcs rm_)
SET(MINIDB_INCLUDE_DIRS ${PROJECT_SOURCE_DIR}/include)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin/lib)

INCLUDE_DIRECTORIES(
    ${MINIDB_INCLUDE_DIRS}
)

# ADD_LIBRARY(rm STATIC ${rm_srcs})
# ADD_LIBRARY(rm SHARED ${rm_srcs})
