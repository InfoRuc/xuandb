SET(MINIDB_INCLUDE_DIRS ${PROJECT_SOURCE_DIR}/include)

INCLUDE_DIRECTORIES(
${MINIDB_INCLUDE_DIRS}
)

# SET(MINIDB_LIBS ${LIBRARY_OUTPUT_PATH}/libpf.a ${LIBRARY_OUTPUT_PATH}/librm.a)
# ADD_EXECUTABLE(target path)
# TARGET_LINK_LIBRARIES()
